{
  "name": "lead-intelligence-platform",
  "version": "1.0.0",
  "description": "Enterprise Lead Intelligence Platform - Phase 1: Business Intelligence Foundation",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"backend,frontend\" -c \"cyan,magenta\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && venv\\Scripts\\python.exe -m uvicorn platform_app.main:app --reload --host 0.0.0.0 --port 8000",
    "install:all": "npm install && cd frontend && npm install",
    "setup": "npm run install:all && cd backend && pip install -r requirements.txt",
    "test:backend": "cd backend && python -m pytest platform_app/tests/ -v",
    "test:frontend": "cd frontend && npm run test",
    "test": "concurrently \"npm run test:backend\" \"npm run test:frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
